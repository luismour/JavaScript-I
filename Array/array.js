const cartas = [];

const novasCartas = cartas.push("Carta1", "Carta2", "Carta 3");
for (let index = 0; index < cartas.length; index++) {
  const element = cartas[index];
  console.log(element + "se encontra posição");
}
